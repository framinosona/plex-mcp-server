services:
  plex-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plex-mcp-server
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - /app/data/secrets/plex-mcp-server/dev.env
